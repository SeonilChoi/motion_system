# utils_pkg

## `sine_motor_command_publisher`

Publishes a synthetic **`motor_command`** (`MotorStatus`) at a fixed rate so you can test **`motor_manager_node`** or the EtherCAT stack without a real teleop source.
