# robot_manager (stack)

Python robot stack wrapped by `ament_cmake`. See **`core/robot_interface/`** for `Robot` / `Scheduler`, **`robot_manager/`** for YAML loading, plus **`kinematics/`**, **`planner/`**, **`scheduler/`**, **`robots/`** packages.
